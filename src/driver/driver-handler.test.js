'use strict';

// I need to go over the class again to make sure I understand what is going on within these tests before I can move forward


// const { events, EVENT_NAMES }  = require("../events")
// const { toTest: { deliver },
// } = require("../driver/handler")

// jest.useFakeTimers();

// test("Delivery", ()=>{
//     const eMock = jest.spyOn(events,"emit")

//     deliver("maru");

//     expect(eMock).toHaveBeenCalledWith(EVENT_NAMES.delivered, "maru");
// });

